# Firebase Studio App - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - only commit .env.example

# ============================================
# Google Gemini API Key (for AI report generation)
# ============================================
# IMPORTANT: This MUST be a Gemini API key, NOT a Firebase API key!
# Get one at: https://aistudio.google.com/apikey
GOOGLE_GENAI_API_KEY=your_gemini_api_key_here

# ============================================
# Firebase Client Configuration (Public - Safe to expose)
# ============================================
# These values are safe to expose in client-side code
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id

# ============================================
# Firebase Admin SDK (Server-side - PRIVATE!)
# ============================================
# ⚠️ CRITICAL: This contains your private key - NEVER commit this!
# To get a service account key:
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate New Private Key"
# 3. Download the JSON file
# 4. Run: ./setup-service-account.sh path/to/your-service-account.json
# OR manually minify and paste the JSON below (keep it as a single line)
FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"...paste_your_service_account_json_here..."}'
